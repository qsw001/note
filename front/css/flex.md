flex与传统的前端display不同，他让父容器接管子元素的“排布规则”，把「怎么排」这件事从子元素身上收回来。

在这里，子元素只用按照父元素里搜指定的规则行事

- 主轴（main axis）：即横向的轴
- 交叉轴（cross axis）：即纵向的轴
---
- justify-content	主轴方向怎么排
- align-items	交叉轴怎么对齐
- flex-wrap	能不能换行
---
他还引入了弹性尺寸的概念，在子元素里，flex：1指的是在剩余的空间里按权重分配比例
