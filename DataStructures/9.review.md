1. 字串一定包含在主串中
2. 两个串不能像数值一样比大小
3. 哈希函数的值域必须在表长范围内
4. 排序方法中关键字比较次数与记录初始状态无关的是简单选择排序
5. 排序的趟数与初始状态无关的是：直接插入排序，堆排序，简单选择排序
6. 串是一种特殊的线性表，其特殊性体现在数据元素是一个字符
7. 串中的元素只能是字母这句话是错的
8. 串既可以用顺序表存储也可以用链表存储
9. ‍二叉树就是结点度为2的有序树。这句话是**错的**，度数小于等于2。